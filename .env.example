# Kopie als .env anlegen und Werte setzen (.env ist gitignored).
# App-Passwort fuer den Login:
APP_PASSWORD=changeme
# Stabiles Secret zum Signieren des Login-Cookies. NICHT aendern,
# sonst werden alle bestehenden Logins ungueltig.
AUTH_SECRET=bitte-langen-zufallswert-setzen
